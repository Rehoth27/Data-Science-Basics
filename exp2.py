import numpy as np
arr = np.array([[1, 2], [3, 4]])
print("Shape:", arr.shape) # finds the shape like the dimensions of the array 
print("Sum:", arr.sum()) # finds the sum of all the elements in the 2d array
